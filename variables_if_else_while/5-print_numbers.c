#include <stdlib.h>
#include <stdio.h>
/**
 * main - entry point
 * Return: 0 no error
 *
 */
int main(void)
{
	int nombre = 0;

	while(nombre <=9)
	{
	printf("%d", nombre);
	nombre++;
	}
	putchar('\n');
	return (0);
}
